# Ariadne

## 🚀 Quick Start (Reproduce with Docker)
If you have Docker installed, you can run the backbone with one command:

1. **Build the image**:
   ```bash
   docker build -t ariadne .


   docker run --rm ariadne